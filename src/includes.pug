mixin head(title)
  doctype html
  html(lang='ja')
    head
      meta(charset='UTF-8')
      title #{ title } | Manaba Plus
      link(rel='stylesheet', href='style.css')
      script(defer, src='index.js')

    if block
      body
        block
